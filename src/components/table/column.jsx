export default function Column({ field, header, filter, OnPress }) {
    // Este componente não renderiza nada diretamente, ele é usado apenas para definir as propriedades das colunas
    return null;
}